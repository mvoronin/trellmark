---
type: quick
description: Add link dragging between groups
---

# Link dragging between groups

1. Add a dedicated pointer drag handle to bookmark rows, accepting visible group destinations including nested, empty, and folded groups. Preserve normal link clicks, scrolling outside the handle, and the keyboard-accessible move selector.
2. Reuse the existing move operation and its request lifetime/error handling. Move only the source membership; cancel on Escape, pointer cancellation, render, and disposal. Prevent overlapping drags and duplicate moves while pending.
3. Add browser coverage for successful membership moves, cancellation, invalid targets, failures, and touch. Rebuild committed browser artifacts and run `just check`.

Execution uses the skill's inline fallback; no subagents are needed.
